export interface PasswordModalInterface {
  passwordIsSelect: boolean;
  setPasswordIsSelect: (state: boolean) => void;
}

export interface ProfilModaleInterface {
  profilIsSelect: boolean;
  setProfilIsSelect: (state: boolean) => void;
}
